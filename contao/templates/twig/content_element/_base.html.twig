{% extends "@Contao/content_element/_base.html.twig" %}

{% set attributes = attributes|default(attrs()).addClass(getAllResponsiveClasses(data)).mergeWith(attributes|default) %}
{% set attributes = data ? attributes|default(attrs()).mergeWith(attributes|default) %}

{% set inner_classes = getAllInnerContainerClasses(data)|join(" ") %}

{%- block inner %}
    {% if nested_fragments %}
        <div class="content__inside inside {{ inner_classes }} {{ nested_fragments ? getContainerClasses(data.responsiveContainer)|join(" ") }}">{{ parent() }}</div>
    {% else %}
        {{ parent() }}
    {% endif %}
{% endblock -%}

{% block content %}
    <div {{ content_attributes }}>
        {{ block('content')|raw }}
    </div>
{% endblock %}